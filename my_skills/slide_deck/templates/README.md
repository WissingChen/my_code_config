# 风格模板库（Style Templates）

用户命名保存的 slide 风格模板。每个模板一个文件夹，文件夹名即用户给定的风格名。

## 索引

| 风格名 | 适用场景 | 来源实例 | 保存日期 |
|---|---|---|---|
| [国自然基金风格](国自然基金风格/) | 基金/项目汇报的机制流程图、预期方案 pipeline 页（输入→引擎→归因三段式 + 闭环） | room2manip slides slide 12 | 2026-08-13 |
| [瑞士国际主义风格](瑞士国际主义风格/) | 整套 deck 的视觉体系：模块化网格 + 极细巨字 + 单一 accent（封面/内容/数据/封底全页型） | NSFC 申请汇报 deck（mo65jg3kga2my.ok.kimi.link） | 2026-08-13 |

## 模板文件夹结构

- `STYLE.md` — 风格规范：风格 DNA、色板 token、布局数值、组件清单、图标库、QA 坑点记录。
- `template.html` — 可渲染的基准实例（单页自包含 HTML）。复刻时复制其中的 CSS 块与 HTML 骨架，只替换内容。

## 维护规则

1. **使用**：用户点名某风格时，先读对应 `STYLE.md`，按 `template.html` 骨架复刻；不凭记忆重写样式。
2. **新增**：从已通过视觉 QA 的 slide 中提取 CSS 变量与组件骨架 → 新建 `templates/<风格名>/STYLE.md` + `template.html` → 在本索引登记（风格名、适用场景、来源实例、日期）。
3. **修订**：风格的改进回写到模板文件（单一事实源），并更新索引中的日期；套用页面只改内容，不改 token 定义。
4. **QA**：每次新增或修订模板后，必须用 headless Chromium 截图检查 `template.html`（1600×900），把新发现的坑写进 `STYLE.md` 的 QA 记录。
